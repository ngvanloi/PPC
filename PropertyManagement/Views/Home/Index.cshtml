@model IEnumerable<PropertyManagement.Models.Property>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<div class="slider" style="width:80%;margin:0 auto;">
    <div id="myCarousel" class="carousel slide" data-ride="carousel">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
            <li data-target="#myCarousel" data-slide-to="1"></li>
            <li data-target="#myCarousel" data-slide-to="2"></li>
        </ol>

        <!-- Wrapper for slides -->
        <div class="carousel-inner" style="width:100%">
            <div class="item active">
                <img src="~/images/ppc0001.jpg" alt="avatar" style="width:100%;height:100%;">
            </div>

            <div class="item">
                <img src="~/images/ppc0002.jpg" alt="ppc2" style="width:100%;height:100%;">
            </div>

            <div class="item">
                <img src="~/images/ppc0003.jpg" alt="ppc3" style="width:100%;height:100%;">
            </div>
        </div>

        <!-- Left and right controls -->
        <a class="left carousel-control" href="#myCarousel" data-slide="prev">
            <span class="glyphicon glyphicon-chevron-left"></span>
            <span class="sr-only">Previous</span>
        </a>
        <a class="right carousel-control" href="#myCarousel" data-slide="next">
            <span class="glyphicon glyphicon-chevron-right"></span>
            <span class="sr-only">Next</span>
        </a>
    </div>
</div>
<h2>Property</h2>
<div class="content" style="width:80%;margin:50px auto;display:grid;grid-template-columns:repeat(auto-fit,minmax(20rem,1fr));grid-gap: 1em;">
    @foreach (var item in Model)
    {
        <div class="card" style="width:20rem;border:1px solid">
            <img src="~/images/@item.Avatar" class="card-img-top" alt="avatar" style="width:100%">
            <div class="card-body">
                <h5 class="card-title">@item.Property_Name</h5>
                <p class="card-text">@item.Description</p>
                <a href="#" class="btn btn-primary" style="display:block;margin:0 auto;">More</a>
            </div>
        </div>
    }
</div>

<h2>Installment</h2>
<div class="content" style="width:80%;margin:50px auto;display:grid;grid-template-columns:repeat(auto-fit,minmax(20rem,1fr));grid-gap: 1em;">
    @foreach (var item in Model)
    {
        <div class="card" style="width:20rem;border:1px solid">
            <img src="~/images/@item.Avatar" class="card-img-top" alt="avatar" style="width:100%">
            <div class="card-body">
                <h5 class="card-title">@item.Property_Name</h5>
                <p class="card-text">@item.Description</p>
                <a href="#" class="btn btn-primary" style="display:block;margin:0 auto;">More</a>
            </div>
        </div>
    }
</div>

<h2>Full-Contract</h2>
<div class="content" style="width:80%;margin:50px auto;display:grid;grid-template-columns:repeat(auto-fit,minmax(20rem,1fr));grid-gap: 1em;">
    @foreach (var item in Model)
    {
        <div class="card" style="width:20rem;border:1px solid">
            <img src="~/images/@item.Avatar" class="card-img-top" alt="avatar" style="width:100%">
            <div class="card-body">
                <h5 class="card-title">@item.Property_Name</h5>
                <p class="card-text">@item.Description</p>
                <a href="#" class="btn btn-primary" style="display:block;margin:0 auto;">More</a>
            </div>
        </div>
    }
</div>

